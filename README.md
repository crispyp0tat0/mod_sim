run these commands
```
python -m venv venv

./venv/Scripts/activate.bat
or
source /venv/bin/activate
if you're in linux

pip install -r requirements.txt

flask --app app init-db

flask --app app run --debug to begin running the app on debug mode
```
